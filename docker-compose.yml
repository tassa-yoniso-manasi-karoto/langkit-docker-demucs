services:
  demucs:
    image: ghcr.io/tassa-yoniso-manasi-karoto/langkit-demucs:latest
    container_name: langkit-demucs
    volumes:
      - ./input:/data/input
      - ./output:/data/output
      - ./models:/data/models
    restart: unless-stopped
    # GPU support via NVIDIA Container Toolkit (optional)
    # Uncomment below for GPU acceleration:
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: all
    #           capabilities: [gpu]
